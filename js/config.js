// js/config.js

// Define the icon styling method for the header navigation.
// true = Use CSS grayscale filter (only need color icons).
// false = Use separate gray/green image files (need both color and gray icons).
const USE_GRAYSCALE_FILTER = true;